.page-user {
  .style-list {
    list-style: none;
    .item {
      padding: 10px;
      overflow: hidden;
      margin-top: 20px;
      border-bottom: 1px dotted #f2f2f2;

      div {
        width: 25%;
        float: left;
      }

      .slug {
        text-align: left;
      }
      .desc {
        color: #999;
        text-align: left;
      }
    }
  }
}